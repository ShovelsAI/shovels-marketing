{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "python3 -m pip install -r requirements.txt && npm install",
  "buildCommand": "npm run build:css:prod && python3 -m pelican content -o docs -s publishconf.py && cp themes/shovels/static/css/output.css docs/output.css && python3 generate_404.py",
  "outputDirectory": "docs"
}
